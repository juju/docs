(metric)=
# Metric

> See also: {ref}``juju collect-metrics` <6463md>`

In Juju, a **metric** is a key/value pair defined by a charm. Typically the value is numeric.

Juju polls charms every 5 minutes via the `juju collect-metrics` {ref}`hook <hook>`.