(binding)=
# Binding

> See also: {ref}``juju deploy --bind` <6488md>`, {ref}``juju bind` <6488md>`

In Juju, {ref}`application <application>` {ref}`endpoints <endpoint>` can be bound to {ref}`spaces <space>`. By binding endpoints to different spaces, Juju users can isolate the traffic used by relation endpoints.