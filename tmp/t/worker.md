(worker)=
# Worker

In a Juju {ref}`deployment <deploying>`, a **worker** is a process that a Juju {ref}`agent <agent>` runs in the background on a Juju entity ({ref}`controller <controller>`, {ref}`model <model>`, {ref}`machine <11298md>`, {ref}`unit <unit>`, etc.) and which has responsibilities related to that entity.